# 更新记录

本文档记录用户可见的功能、性能和安全性变化。版本号遵循语义化版本。

## 2.1.1 - 2026-08-09

### 分发修复

- GitHub Release 现在同时提供可直接双击运行的独立 x64 EXE 和包含说明文件的便携 ZIP，不再让用户误下载 GitHub 自动生成的源码包。
- 为独立 EXE 和便携 ZIP 分别生成 SHA-256 校验文件。
- 新增 Windows GitHub Actions 发布流程；推送 `v*` 标签后自动编译原生程序、创建 Release 并上传四个真实构建产物。
- 构建产物继续使用静态 MSVC 运行库，不要求用户额外安装 VC++ Redistributable。

## 2.1.0 - 2026-08-04

### 新增

- 原生 DirectX 版本新增代理光标：读取当前 Windows 光标形状与热点，将光标合成到桌面源坐标，再与画面一起通过相同的 HLSL 径向变换。
- 使用 Windows Magnification API 临时隐藏真实系统光标；底层鼠标仍停留在真实逻辑坐标，不拦截或重新注入输入事件。
- 全屏覆盖层使用 `WS_EX_LAYERED | WS_EX_TRANSPARENT` 实现跨进程点击穿透，独立控制面板仍可正常交互。
- 新增 `native/tests/input-passthrough.ps1`，用真实鼠标输入验证位于覆盖层下方的独立进程能够收到 `MouseDown`。
- 新增 60 FPS 原生自检模式 `--smoke-test-60`，自检日志记录目标帧率、代理光标准备状态和覆盖层输入样式。

### 性能

- 使用 `QueryPerformanceCounter` 和高精度 waitable timer 替换毫秒级帧调度。
- 交换链使用 `SyncInterval=0`，并将最大排队帧数限制为 1，避免垂直同步等待与软件限帧叠加。
- 桌面捕获按所选 30/60 FPS 调度，代理光标位置独立按 60 Hz 检查；桌面与光标均未变化时跳过着色和 Present。
- 光标不覆盖当前像素时跳过光标纹理采样。相同 8 秒自检中，30 FPS 模式由约 20.35 FPS 提升到约 30 FPS；60 FPS 模式稳定在约 60 FPS。

### 安全与修复

- 独立看门狗记录光标隐藏状态，在主进程崩溃、卡死或未能及时清理时恢复系统光标。
- 在旁路、正常退出、紧急退出、睡眠、会话结束、显示模式变化、鼠标离开当前显示器或进入控制面板时恢复真实光标。
- 修复全屏 DirectX 覆盖层可能拦截桌面和其他程序点击的问题。
- 代理光标准备或隐藏失败时保留真实光标，避免双光标或无光标状态。

### 文档

- 重写项目作用说明，补充径向反畸变公式、`k1`/`k2` 调整顺序、模型适用条件及局限。
- 补充覆盖层焦点、键鼠输入、代理光标、帧调度和异常恢复的实现说明。

## 2.0.1 - 2026-08-02

- 补充项目作者与发布者信息。

## 2.0.0 - 2026-08-02

- 新增 Win32、DXGI Desktop Duplication、Direct3D 11 和 HLSL 实现的原生反畸变运行时。
- 新增安全旁路、全局退出热键、托盘入口和独立看门狗。

## 1.0.0

- 提供 Electron/WebGL 桌面黑洞与反畸变便携版本。
